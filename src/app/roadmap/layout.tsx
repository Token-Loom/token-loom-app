import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Roadmap - TokenLoom',
  description:
    'Future development plans and upcoming features for TokenLoom - The secure Solana token burning platform.'
}

export default function RoadmapLayout({ children }: { children: React.ReactNode }) {
  return children
}
